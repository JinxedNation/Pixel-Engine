#include "Factory_GameAsset.h"


PixelEngine::Object* PixelEngine::Factory_GameAsset::generate_ObjectType(const std::string& temp_ObjectType)
{
    /**************************
    if(temp_ObjectType == "NPC")
    {
        return new //NPC();
    }
    else 
        if(temp_ObjectType == "Lake")
        {
            return new //Lake();
        }

    return nullptr;
    ***********/
    return nullptr;
    
}
