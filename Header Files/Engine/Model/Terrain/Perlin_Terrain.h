#include <iostream>
#include <fstream>

// call openGL loader
/****

// (http://libnoise.sourceforge.net/downloads/index.html)  -> library file for perlin noise

namespace PixelEngine
{
    class Perlin_Terrain
    {
        public:
                
                /**************************************************************************************************
                 * @brief  : Perlin_Terrain Destructor
                 * @brief  : Destructor is virtual as this will allow the Terrain object to also be deleted through a pointer to the base class
                 * @return : ? deleted object
                 ************************************************************************************************** 

            Perlin_Terrain(const std::string tempName, int tempWidth, int tempHeight, float tempScale, int tempRandomSeed);
        

                /**************************************************************************************************
                 * @brief  : Perlin_Terrain Destructor
                 * @brief  : Destructor is virtual as this will allow the Terrain object to also be deleted through a pointer to the base class
                 * @return : ? deleted object
                 ************************************************************************************************** 

            virtual ~PerlinTerrain();

        private:
        
                /**************************************************************************************************
                 * @brief  : generateTerrain 
                 * @brief  : used to create the perlin noise 3D terrain
                 * @return : ? deleted object
                 ************************************************************************************************** 

            void spawnTerrain(int tempRandom);
    };
}
**/